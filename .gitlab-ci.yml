image: mcr.microsoft.com/playwright:v1.42.1-jammy

stages:
  - test

playwright_test:
  stage: test
  script:
    - npm ci
    - npx playwright install --with-deps
    - npx playwright test
  artifacts:
    when: always
    paths:
      - playwright-report/
    expire_in: 1 week